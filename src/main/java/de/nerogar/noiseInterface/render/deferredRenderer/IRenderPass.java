package de.nerogar.noiseInterface.render.deferredRenderer;

import de.nerogar.noise.render.deferredRenderer.SimpleRenderableContainer;

public interface IRenderPass {

	SimpleRenderableContainer getContainer();

}
